"""Autogenerated OpenBB /derivatives/futures Module."""

### THIS FILE IS AUTO-GENERATED. DO NOT EDIT. ###

#  pylint: disable=R0917,C0103,C0415

from openbb_core.app.static.container import Container
from openbb_core.app.model.obbject import OBBject
import openbb_core.provider
from openbb_core.provider.abstract.data import Data
import pandas
from pandas import DataFrame, Series
import numpy
from numpy import ndarray
import datetime
from datetime import date
import pydantic
from pydantic import BaseModel
from inspect import Parameter
import typing
from typing import TYPE_CHECKING, Annotated, ForwardRef, Union, Optional, Literal, Any
from annotated_types import Ge, Le, Gt, Lt
from warnings import warn, simplefilter
from openbb_core.app.static.utils.decorators import exception_handler, validate

from openbb_core.app.static.utils.filters import filter_inputs

from openbb_core.app.deprecation import OpenBBDeprecationWarning

from openbb_core.app.model.field import OpenBBField
from fastapi import Depends
import openbb_core.app.model.command_context
import openbb_core.app.provider_interface

from openbb_core.app.model.command_context import CommandContext
from openbb_core.app.provider_interface import (
    OBBject_FuturesCurve,
    OBBject_FuturesHistorical,
)

class ROUTER_derivatives_futures(Container):
    """/derivatives/futures
    curve
    historical
    """

    def __repr__(self) -> str:
        return self.__doc__ or ""

    @exception_handler
    @validate
    def curve(
        self,
        symbol: Annotated[
            Union[str, Literal['VX_AM', 'VX_EOD']],
            OpenBBField(
                description=(
                    'Symbol to get data for.;     Symbol to get data'
                    "for.Default is 'VX_EOD'. Entered dates return the"
                    "data nearest to the entered date.     'VX_AM' ="
                    "Mid-Morning TWAP Levels     'VX_EOD' = 4PM Eastern"
                    'Time Levels (provider: cboe)'
                )
            )
        ],
        date: Annotated[
            Union[datetime.date, str, None, list[datetime.date | str | None]],
            OpenBBField(
                description=(
                    'A specific date to get data for. Multiple comma'
                    'separated items allowed for provider(s): cboe,'
                    'yfinance.'
                )
            )
        ] = None,
        provider: Annotated[
            Optional[Literal['cboe', 'yfinance']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: cboe, yfinance.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Futures Term Structure, current or historical.

Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: cboe, yfinance.
symbol : str
    Symbol to get data for.

    (provider: cboe)
        Default is 'VX_EOD'. Entered dates return the data nearest to the entered date.
        'VX_AM' = Mid-Morning TWAP Levels
        'VX_EOD' = 4PM Eastern Time Levels
date : date | str | list[date | None] | None
    A specific date to get data for. Multiple comma separated items allowed for provider(s): cboe, yfinance.

Returns
-------
OBBject
    results : list[FuturesCurve]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

FuturesCurve
------------
date : date | None
    The date of the data.
expiration : str
    Futures expiration month.
price : Optional[float]
    The price of the futures contract.
symbol : str | None
    Symbol representing the entity requested in the data. (provider: cboe)

Examples
--------
>>> from openbb import obb
>>> obb.derivatives.futures.curve(symbol='VX', date='2024-06-25')
>>> obb.derivatives.futures.curve(symbol='NG')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/derivatives/futures/curve",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "derivatives.futures.curve",
                        ('cboe', 'yfinance'),
                    )
                },
                standard_params={
                    "symbol": symbol,
                    "date": date,
                },
                extra_params=kwargs,
                info={'date': {'cboe': {'multiple_items_allowed': True, 'choices': None}, 'yfinance': {'multiple_items_allowed': True, 'choices': None}}},
            )
        )

    @exception_handler
    @validate
    def historical(
        self,
        symbol: Annotated[
            Union[str, list[str]],
            OpenBBField(
                description=(
                    'Symbol to get data for. Multiple comma separated'
                    'items allowed for provider(s): yfinance.'
                )
            )
        ],
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='Start date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='End date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        expiration: Annotated[
            str | None,
            OpenBBField(
                description='Future expiry date with format YYYY-MM'
            )
        ] = None,
        provider: Annotated[
            Optional[Literal['yfinance']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: yfinance.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Historical futures prices.

Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: yfinance.
symbol : str | list[str]
    Symbol to get data for. Multiple comma separated items allowed for provider(s): yfinance.
start_date : date | str | None
    Start date of the data, in YYYY-MM-DD format.
end_date : date | str | None
    End date of the data, in YYYY-MM-DD format.
expiration : str | None
    Future expiry date with format YYYY-MM
interval : str
    Time interval of the data to return. (provider: yfinance)
    Choices for yfinance: '1m', '2m', '5m', '15m', '30m', '60m', '90m', '1h', '1d', '5d', '1W', '1M', '1Q'

Returns
-------
OBBject
    results : list[FuturesHistorical]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

FuturesHistorical
-----------------
date : datetime
    The date of the data.
open : float
    The open price.
high : float
    The high price.
low : float
    The low price.
close : float
    The close price.
volume : float
    The trading volume.

Examples
--------
>>> from openbb import obb
>>> obb.derivatives.futures.historical(symbol='ES')
>>> # Enter multiple symbols.
>>> obb.derivatives.futures.historical(symbol='ES,NQ')
>>> # Enter expiration dates as "YYYY-MM".
>>> obb.derivatives.futures.historical(symbol='ES', expiration='2025-12')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/derivatives/futures/historical",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "derivatives.futures.historical",
                        ('yfinance',),
                    )
                },
                standard_params={
                    "symbol": symbol,
                    "start_date": start_date,
                    "end_date": end_date,
                    "expiration": expiration,
                },
                extra_params=kwargs,
                info={'symbol': {'yfinance': {'multiple_items_allowed': True, 'choices': None}}},
            )
        )
